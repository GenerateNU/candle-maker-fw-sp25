#ifndef STATEMACHINE
#define STATEMACHINE_HPP

enum STATES {
    STANDBY,
    HEATING,
    DISPENSING,
    MIXING,
    WICK_PLACEMENT,
    COOLING,
    EJECTING
};

class StateMachine {
    public:

    private:

};

#endif